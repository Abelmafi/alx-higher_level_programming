The ``0-add_integer`` module
======================

Using ``factorial``
-------------------

This is an example text file in reStructuredText format.  First import
``factorial`` from the ``example`` module:

        >>> from 0-add_integer import add_intiger

Now use it:
        >>> add_integer(1, 2)
        4

